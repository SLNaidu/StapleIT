@model UserListViewModel
@section heads{
    <link rel="stylesheet" href="~/Content/CreateList.css" />
   
     
}

<navbar>
    <div id="header">
        <div class="waviy">
            <span style="--i:1">S</span>
            <span style="--i:2">T</span>
            <span style="--i:3">A</span>
            <span style="--i:4">P</span>
            <span style="--i:5">L</span>
            <span style="--i:6">E</span>
            <span style="--i:7">I</span>
            <span style="--i:8">T</span>
            <span style="--i:8">.</span>
            <a asp-action="Index" asp-controller="Login" style="text-transform: uppercase; padding-top: 20px; color: #fff;" class="pull-right"><b>Logout</b></a>
        </div>


    </div>
    <div class="underline-title"></div>
    <ul id="nav" class="nav nav-tabs">
        <li>
            <a asp-action="Index" asp-controller="Home">Home</a>
        </li>
        <li>
            <a asp-action="Index" asp-controller="List">Lists</a>
        </li>
        <li class="active">
            <a asp-action="Index" class="active" asp-controller="Group"><b>Groups</b></a>
        </li>

    </ul>
</navbar>
<br>
<div class="container-fluid"
<div class="row">
    <div class="col-md-4">
         <form asp-action="AddUser">
             <div class="form-group">
            <label for="sel1" class="form-label">Select User (select one):</label>
            &nbsp;
              <select class="form-control" id="sel1" name="userId">
                  <option> --Select User--</option>
                    @foreach (var v in Model.Users)
                    {
                            <option value="@v.UserId">@(v.FName + " " + v.LName)</option>
                    }
                   
    
                 </select>
             </div>
            <div class="form-group">
                 <input type="hidden" asp-for="@Model.groupId" />
                <a asp-action="Index" class="btn btn-info">Cancel</a>
                <input type="submit" value="Add" class="btn btn-primary" />
            </div>
         </form>
    </div>
</div>
</div>

